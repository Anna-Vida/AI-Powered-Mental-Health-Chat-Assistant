services:
  - type: web
    name: mindchat
    runtime: node
    buildCommand: ''
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_TOKEN
        sync: false